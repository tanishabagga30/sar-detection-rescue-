seed: 42
model_name: openai/clip-vit-base-patch32
output_dir: outputs/clip-sard-adapted

training:
  epochs: 3
  batch_size: 4
  lr: 2.0e-5
  weight_decay: 0.01
  num_workers: 0
  device: auto
  mixed_precision: false
  image_size: 224
  log_every: 20

stage1:
  enabled: false
  dataset_dir: data/rsicd_small

stage2:
  enabled: true
  dataset_dir: data/sard_text